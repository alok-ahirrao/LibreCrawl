# LibreCrawl Docker Environment Configuration

# Host binding for the application
# - 127.0.0.1 = localhost only (local development)
# - 0.0.0.0 = all interfaces (production, accessible from network)
HOST_BINDING=127.0.0.1

# Local mode - enables auto-login with admin access, no rate limits
# Set to "true" for local development, "false" for production
LOCAL_MODE=true

# Logging Mode: 'debug' (verbose), 'info' (default), 'production' (errors only)
LOG_MODE=info

# Disable user registrations
# Set to "true" to prevent new users from registering
# Useful for production environments where you only want existing users
REGISTRATION_DISABLED=false

# Email Configuration for verification emails
# SMTP server settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@librecrawl.com
SMTP_FROM_NAME=LibreCrawl

# Base URLs for email links (used to generate verification links)
MAIN_APP_URL=https://crawl.librecrawl.com
WORKSHOP_APP_URL=https://workshop.librecrawl.com

# Google Gemini AI API Key (free tier: 1500 requests/day)
# Get your key from: https://aistudio.google.com/
# Required for AI-enhanced keyword research features
GEMINI_API_KEY=

# ==================== Google Business Profile (GBP) ====================

# Google OAuth 2.0 Credentials for GBP API
# Create credentials at: https://console.cloud.google.com/apis/credentials
# Required scopes: business.manage, openid, email, profile
GOOGLE_GMB_CLIENT_ID=
GOOGLE_GMB_CLIENT_SECRET=
GOOGLE_GMB_REDIRECT_URI=http://localhost:3000/gbp-manager/callback

# GBP API Rate Limiting
# Requests per second for official GBP API (default: 1.0)
GMB_API_RATE_LIMIT=1.0
# Requests per second for crawler (default: 0.2 = 1 request per 5 seconds)
GMB_CRAWLER_RATE_LIMIT=0.2

# GBP Sync Settings
# How many worker threads for sync processing
GMB_SYNC_WORKERS=3

# GBP Proxy Configuration (for geo-targeted crawling)
GMB_PROXY_ENABLED=false
GMB_PROXY_URL=
GMB_PROXY_ROTATION=false

# GBP Crawler Settings
GMB_CRAWLER_HEADLESS=true
GMB_CRAWLER_TIMEOUT=30000
GMB_CRAWLER_MAX_RETRIES=3

# GBP Cache TTL (seconds)
# API response cache (default: 15 minutes)
GMB_CACHE_TTL_API=900
# SERP result cache (default: 1 hour)
GMB_CACHE_TTL_SERP=3600


# Optional: SERP API Provider for fast mode without browser
# Options: 'scraperapi', 'serpapi', 'none' (default: none - use browser)
SERP_API_PROVIDER=none
SERP_API_KEY=

# Optional: Anti-CAPTCHA service
# Options: '2captcha', 'anticaptcha', 'none' (default: none - manual)
CAPTCHA_PROVIDER=none
CAPTCHA_API_KEY=

